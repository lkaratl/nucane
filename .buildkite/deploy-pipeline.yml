steps:
  - key: "input"
    label: "Input"
    input: "Deploy params"
    fields:
      - key: "namespace"
        select: "Namespace"
        hint: "Namespace for deploy"
        options:
          - label: "dev"
            value: "dev"
          - label: "staging"
            value: "staging"
          - label: "prod"
            value: "prod"
        default: "dev"
      - key: "force"
        select: "Force"
        hint: "Be careful if this option 'true' existing namespace will be force deleted"
        options:
          - label: "true"
            value: "true"
          - label: "false"
            value: "false"
        default: "false"
      - key: "version"
        text: "Version"
        hint: "Value format '\\$BRANCH-\\$BUILD' or 'latest', get exact values from build pipeline"
        default: "latest"
      - key: "params"
        text: "Custom params"
        hint: "Custom environment parameters"
        default: |
          DEMO=true
          OKX_API_KEY=
          OKX_API_PASSPHRASE=
          OKX_API_SECRET=
  - key: "propagate"
    depends_on: "input"
    label: "Params"
    commands:
      - "NAMESPACE=$(buildkite-agent meta-data get namespace) && echo \"NAMESPACE: $$NAMESPACE\""
      - "FORCE=$(buildkite-agent meta-data get force) && echo \"FORCE: $$FORCE\""
      - "VERSION=$(buildkite-agent meta-data get version) && echo \"VERSION: $$VERSION\""
      - |
        while IFS= read -r line
        do
            echo "$$line"
            export $$line
        done <<< "$(buildkite-agent meta-data get params)"
  - key: "Deploy"
    depends_on: "propagate"
    label: ":mag: Deploy"
    commands:
      - "docker compose up -d"
